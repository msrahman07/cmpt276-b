<%= stylesheet_link_tag 'application' %>

<% provide(:title, 'Sign up') %>
<h1>Sign up</h1>

<div class="row">
  <div>
    <%= form_for(@user) do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :email %>
      <%= f.email_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.submit "Create my account", id: 'submit' %>
    <% end %>
  </div>
</div>